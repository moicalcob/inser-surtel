pipeline {
    // agent { dockerfile true }
    stages {
        // stage('Test') {
        //     steps {
        //         sh 'ls'
        //     }
        // }

        stage('Run Script'){
            steps {
                script {
                    sh('ls')
                }
            }
        }
    }
}